# Secrets & local env (never commit real keys)
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example
frontend/.env
frontend/.env.*
!frontend/.env.example

# Local Python virtualenv
env/
.venv/
venv/

# Curated local snapshot / archives (not the runtime app)
main_scripts/
main_scripts.zip
*.zip

# Root duplicates / scratch (canonical code lives under backend/)
/plan_generator.py
/rag/
frontend_*_snippet.jsx
BUGFIX_CHANGELOG.md
README_REFACTOR.md
EXERCISE_DATA_MERGE_REPORT.md
SETUP_AND_TEST_GUIDE.md

# Python
**/__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
*.log

# Node
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/

# Local vector DB & SQLite (rebuild via scripts)
qdrant_local/
**/qdrant_local/
**/qdrant_local.bak/
rag/qdrant_local
backend/rag/qdrant_local/
backend/data/qdrant/
**/fitness.db
**/*.db
*.db-journal

# Unused / duplicate data relocated from data/ (do not commit)
stale_data/

# Local learning / study materials (keep on disk, do not commit)
docs/

# Nested media dataset (too large for git — clone/symlink separately)
data/hasaneyldrm-exercises-dataset/

# Extracted guideline PDF demos (rebuild via backend/rag/extract_pdf_images.py)
backend/static/guideline_images/
**/guideline_images/

# Large / regenerated dumps (rebuild with backend scripts)
exercises.json
**/exercises.json
INDB.xlsx
**/INDB.xlsx
**/*exercises_merged.json
**/all_chunks.json
**/pdf_images_map.json
**/chunks/
data/adaptive-fitness-planner-data/chunks/

# PDFs: ignore globally, but track guideline sources under data/
*.pdf
!data/**/*.pdf

# Keep small schema examples if any
!**/exercises.schema.json
# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp
*~

# Eval / agent temp
.tmp/
.temp/
**/agent-transcripts/
